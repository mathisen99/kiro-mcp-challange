# Scripts

Add scripts only when they remove repeated, verified manual steps.

Good candidates after implementation:

- clean real smoke test;
- final tracked-file hygiene check;
- demo preflight.

Do not add shell wrappers that hide failing build or battle commands.
