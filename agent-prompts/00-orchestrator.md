# Orchestrator prompt

You are implementing Kiro Royale in this repository.

Read `AGENTS.md`, `KIRO_ROYALE_AGENT_BUILD_BRIEF.md`, the three files under
`.kiro/specs/kiro-royale/`, `STATUS.md`, and `DECISIONS.md`.

Work autonomously through the next incomplete numbered stage only. Inspect the
repository before changing it. Verify current official APIs and versions. Run all
commands needed to prove the stage exit condition. Update `STATUS.md` and
`DECISIONS.md` with exact evidence.

Do not stop after scaffolding. Do not use fake battle data. Do not begin stretch
features. Preserve stdout for MCP JSON-RPC in MCP mode.

At the end, summarize:

- files changed;
- commands run and exit codes;
- what is genuinely verified;
- what remains unverified;
- the next stage.
